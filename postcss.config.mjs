/**
 * POSTCSS CONFIGURATION
 *
 * Configured for Tailwind CSS v4.
 * The '@tailwindcss/postcss' plugin handles the processing of modern CSS
 * features and Tailwind-specific directives like '@theme'.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
